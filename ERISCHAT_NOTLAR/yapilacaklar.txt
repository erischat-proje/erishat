ERISCHAT — GÜNCEL YAPILACAKLAR

1. CI / kalite
- Gerçek runner E2E zinciri doğrulandı: anonymous session, room, ban, DM ve cosmetic smoke testleri geçti.
- Quality workflow gerçek runner'da başarıyla tamamlandı.
- Railway deploy kullanıcı açıkça "deploy" demeden yapılmayacak.

2. GitHub Pages
- Aktif hedef workflow: .github/workflows/erischat-pages.yml
- Eski .github/workflows/pages.yml kaldırıldı; duplicate Pages deployment workflow'u artık tutulmuyor.
- pages-diagnostics.yml aktif Pages workflow'unu doğrulayacak şekilde hizalı.
- Gerçek Pages state tanısında Pages aktif/built, public ve github-pages environment mevcut olarak doğrulandı.
- github-pages environment gerçek runner/API çıktısında `branch_policy`, `protected_branches=false`, `custom_branch_policies=true` olarak doğrulandı.
- Bağımsız environment-probe job'ı gerçek runner'da `failure` + `steps: []` ile runner'a ulaşamadı; aynı workflow'daki environment'sız diagnostics job'ı SUCCESS oldu. Gate'in environment admission katmanında olduğu izole edildi.
- Environment kaldırılarak yapılan önceki izolasyonda runner çalıştı fakat deploy-pages "Missing environment" ile durdu. Yani deploy için github-pages environment zorunlu.
- Gerçek diagnostics API çıktısında `github-pages` custom deployment branch policy listesinde yalnızca `gh-pages` bulunuyor; `main` izinli değil.
- `pages-diagnostics.yml` artık `target_branch_allowed=false` olduğunda açık hata ile fail ediyor; policy engeli sessiz SUCCESS olarak raporlanmayacak.
- Bu guard commit'i: `08b455c30b0452ee523df166fb499666b3870dd7`.
- Son Pages workflow temizliğinde Pages workflow'unun injection listesinde gift-live.js tekrar eklenmeyecek şekilde düzenlendi; frontend kaynak dosyası ayrıca mevcut.
- Pages PASS/TAMAM denmeyecek; gerçek başarılı deploy ve canlı HTTP doğrulaması şart.
- Pages başarıyla deploy olduktan sonra canlı frontend HTTP ve frontend -> Railway API bağlantısı gerçek ortamda doğrulanmalı.

3. Backend / güvenlik
- Backend kalite kontrolleri ve room security regression korunacak.
- CORS wildcard yalnızca kontrollü şekilde ele alınacak; frontend gerçek Pages origin doğrulanmadan kırıcı değişiklik yapılmayacak.
- Oda, DM, anonim oturum, kozmetik ve WebSocket akışları için mevcut gerçek smoke testleri korunacak.

4. Kozmetik
- Aktif katalog gerçek Gereken_icerikler klasörlerindeki geçerli assetlerden oluşur.
- Son doğrulanan aktif kozmetik sayısı: 139.
- Eski 40/40/12/12 hedefi zorunlu envanter kuralı değildir.

5. Legacy temizliği
- Kullanılmayan eski workflow/probe dosyaları temizlenecek.
- Not dosyaları her gerçek doğrulama sonrasında senkron tutulacak.

SON DURUM
- Son güvenilir kalite kanıtı: gerçek runner'da 5 smoke test başarılı.
- Pages diagnostics gerçek Pages state'i okuyabiliyor.
- Pages environment gate gerçek runner'da izole edildi ve custom policy'nin gerçek girdisi alındı: yalnızca `gh-pages`.
- Sıradaki kritik iş: GitHub `github-pages` environment custom deployment branch policy'sine `main` eklemek veya policy'yi `main`i kapsayacak şekilde GitHub Settings'ten değiştirmek; ardından gerçek Pages deploy + canlı HTTP + Pages→Railway API doğrulaması.
