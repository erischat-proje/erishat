# ErisChat Son Durum — 2026-09-17

## Son checkpoint
- Repo: `semihvarolymk-png/erischat`, branch: `main`.
- Railway `erischat-web-fixed` tarafında repo + servis config hizalaması yapıldı: Dockerfile path=`Dockerfile`, healthcheck=`/`, `PORT=80`; deploy yapılmadı.
- Caddyfile repo tarafında `:80` portuna sabit; Dockerfile `EXPOSE 80` kullanıyor. Canlı 502'nin yeni config ile düzelip düzelmediği deploy edilmeden doğrulanamaz.
- Railway production deploy yalnızca kullanıcı açıkça `deploy` dediğinde yapılacak.

## Kozmetik tam aktif envanter modeli
- Sabit 40/40/12/12 hedefleri artık geçerli değil; `Gereken_icerikler` altındaki altı katalog klasöründe bulunan tüm geçerli assetler aktiftir.
- Son doğrulanmış envanter: 34 kadın avatar + 37 erkek avatar = 71 standart avatar; 32 standart frame; 12 kadın VIP avatar + 12 erkek VIP avatar = 24 VIP avatar; 12 VIP frame; toplam 139 aktif asset.
- `backend/app/cosmetics.py` gerçek klasörlerden katalog üretir. Inventory checker da gerçek klasör envanterini doğrular.

## Gerçek live E2E checkpoint
- GitHub-hosted runner üzerinde gerçek E2E başarıyla doğrulandı: run `35232358149`.
- `live-e2e` job `105239447055` SUCCESS; probe job `105239447316` SUCCESS.
- Aynı gerçek koşuda şu beş adımın tamamı SUCCESS oldu: Anonymous session, Room, Room ban, DM, Cosmetic.
- Anonymous smoke; register → `/me` → public profile → profile patch → notification patch → logout → token sonrası 401 zincirini doğruluyor.
- Room smoke; iki session, room create/join, gift/balance ve iki istemcili room WebSocket chat akışını doğruluyor.
- Ban smoke; ban → liste → 403 rejoin → unban → yeniden join akışını doğruluyor.
- DM smoke; conversation create/list, message send ve history akışını doğruluyor.
- Cosmetic smoke; catalog → balance → purchase → ownership → apply akışını doğruluyor.

## WebSocket / frontend düzeltmeleri
- Room WebSocket route interception problemi için frontend static mount sırası düzeltildi; route-fix workflow gerçek runner'da SUCCESS oldu.
- Room smoke harness'ında local `websocket-client` proxy bypass düzeltmesi commit `8f065859587a47a575f92be87b509819b376557e` ile yapıldı ve yukarıdaki gerçek E2E koşusunda doğrulandı.
- `frontend/auth-live.js` anonymous kayıt artık backend sözleşmesine uygun `gender: 'male'` gönderiyor; güncel blob SHA `d582fcc8cec9a0072cdfa2d259948d42a193b0e3`.

## CI / Pages checkpoint
- `.github/workflows/runner-recovery.yml` aynı beşli zinciri içeriyor ve gerçek runner üzerinde SUCCESS doğrulaması mevcut.
- `erischat-quality.yml` sadeleştirilerek runner-recovery ile doğrulanmış çalışma modeline hizalandı; run `35234028161` için gerçek `quality` job başarıyla doğrulandı.
- Eski karmaşık quality workflow'unda `jobs: []` görülmesi için artık rastgele kod değişikliği yapılmıyor; önce sade çalışan taban doğrulandı.
- Eski `pages.yml` kaldırıldı. Aktif Pages deployment workflow'u `.github/workflows/erischat-pages.yml` ve tek başına build/upload/deploy yapıyor.
- Pages workflow temizliğinde `gift-live.js` Pages injection listesinde duplicate olmaması için çıkarıldı; frontend kaynakta `gift-live.js` ayrı dosya olarak bulunuyor ve injection listesinde tekrar eklenmiyor.
- `pages-diagnostics.yml` aktif `erischat-pages.yml` için configure/upload/deploy, permissions ve `github-pages` environment kontrollerini yapıyor.
- Gerçek Pages diagnostics runner logu: Pages enabled=`true`, repo public, Pages status=`built`, URL=`https://semihvarolymk-png.github.io/erischat/`.
- Aynı gerçek diagnostics logunda `github-pages` environment protection rule tipi `branch_policy`; deployment policy `protected_branches=false`, `custom_branch_policies=true` olarak kanıtlandı.
- Kritik izolasyon: `github-pages` environment'a bağlanan bağımsız `environment-probe` job'ı gerçek runner'da `failure` + `steps: []` ile runner step'lerine hiç ulaşmadı. Aynı workflow'daki environment'sız `diagnostics` job'ı gerçek runner'da SUCCESS oldu. Bu, gate'in Pages action/build içinden değil environment admission katmanından önce olduğunu doğrudan kanıtlıyor.
- Yeni committe environment-probe kalıcı workflow'dan çıkarıldı; probe yalnızca izolasyon kanıtı üretmek için kullanıldı.
- Environment kaldırılarak yapılan önceki izolasyonda runner çalışmış, fakat `actions/deploy-pages@v4` "Missing environment" ile durduğu da önceki gerçek kanıt olarak korunuyor. Dolayısıyla deploy için `github-pages` environment zorunlu, fakat mevcut environment branch-policy gate'i `main` job'unu runner'a almıyor.
- `pages-diagnostics` şu an environment custom branch policy girdilerini de API üzerinden raporlayacak şekilde güncellendi; bir sonraki gerçek run bu girdilerin hangi branch adlarını kapsadığını gösterecek.
- Pages PASS/TAMAM denmiyor; gerçek başarılı deploy ve canlı HTTP doğrulaması şart.

## Güncel sıra
1. `github-pages` custom branch policy girdilerinin bir sonraki gerçek diagnostics run logundan alınması.
2. Policy'nin `main` ile uyumlu hale gelmesini sağlayacak gerçek GitHub environment ayarı doğrulanmalı; mevcut GitHub connector bu environment yönetim yazma endpointini sunmuyor, bu yüzden ayar repo workflow dosyasından tahmin edilerek değiştirilmemeli.
3. Environment gate çözüldüğünde yeni sade Pages workflow'un gerçek başarılı deploy job/run sonucunu doğrula.
4. Pages canlı HTTP + Pages→Railway API doğrulaması; Railway deploy kullanıcı açıkça `deploy` demeden yapılmayacak.
5. Backend security + quality hardening.
6. V35/V53 legacy cleanup.
7. TXT checkpoint senkronizasyonunu koru.

## Kural
- Canlı/local E2E çalıştırılmadan özellik tamamlandı sayılmayacak.
- Gerçek E2E PASS kanıtı olmadan PASS/TAMAM denmeyecek.
- Railway production deploy yalnızca kullanıcı açıkça `deploy` dediğinde yapılacak.
- Pages kök nedeni log/API kanıtı olmadan tahmin edilmeyecek.
