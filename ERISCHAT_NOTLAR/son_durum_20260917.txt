# ErisChat Son Durum — 2026-09-17

## Son checkpoint
- Repo: `semihvarolymk-png/erischat`, branch: `main`.
- Railway `erischat-web-fixed` tarafında repo + servis config hizalaması yapıldı: Dockerfile path=`Dockerfile`, healthcheck=`/`, `PORT=80`; deploy yapılmadı.
- Caddyfile repo tarafında `:80` portuna sabit; Dockerfile `EXPOSE 80` kullanıyor. Canlı 502'nin yeni config ile düzelip düzelmediği deploy edilmeden doğrulanamaz.
- Railway production deploy yalnızca kullanıcı açıkça `deploy` dediğinde yapılacak.

## Kozmetik tam aktif envanter modeli
- Sabit 40/40/12/12 hedefleri artık geçerli değil; `Gereken_icerikler` altındaki altı katalog klasöründe bulunan tüm geçerli assetler aktiftir.
- Son doğrulanmış envanter: 34 kadın avatar + 37 erkek avatar = 71 standart avatar; 32 standart frame; 12 kadın VIP avatar + 12 erkek VIP avatar = 24 VIP avatar; 12 VIP frame; toplam 139 aktif asset.
- `backend/app/cosmetics.py` gerçek klasörlerden katalog üretir. Inventory checker da gerçek klasör envanterini doğrular.

## Live E2E / WebSocket bulgusu
- Gerçek GitHub Actions runner çalıştığı yeni minimal recovery workflow ile doğrulandı: run `35227284860`, job `105221988844`, probe adımları SUCCESS.
- Önceki gerçek live E2E koşusunda PostgreSQL, static quality, API readiness ve REST room/gift akışı geçti; ilk başarısız nokta iki istemcili room WebSocket handshake oldu. Bu yüzden ban/DM/cosmetic adımları o koşuda çalışmadı.
- Sonraki push'larda mevcut eski quality/apply workflow'larının bazı run kayıtları `failure` + `jobs: []` şeklinde oluştu. Bunlar runner çalışmıyor kanıtı değildir; ayrı recovery runner job'ı gerçek runner üzerinde SUCCESS olduğundan runner erişimi doğrulanmıştır. Eski workflow run'larının neden job üretmediği ayrıca araştırılacak; tahmin edilmeyecek.
- WebSocket smoke harness'ında local E2E için `websocket-client` environment proxy'sini açıkça bypass edecek düzeltme yapıldı: commit `8f065859587a47a575f92be87b509819b376557e`.
- Düzeltme henüz gerçek live E2E'de PASS olarak doğrulanmadı.

## Güncel E2E sırası
1. `room_live_smoke.py` WebSocket proxy bypass düzeltmesini gerçek runner üzerinde doğrula.
2. Room ban → liste → unban → yeniden katılım E2E.
3. İki istemcili room WebSocket chat E2E.
4. Gift gönderimi + bakiye + %70 alıcı payı + room_gift event E2E.
5. Cosmetic purchase → ownership → apply E2E.
6. DM conversation create/list/send/history E2E.
7. Anonymous/session/profile/logout E2E.
8. Pages canlı HTTP + Pages→Railway API doğrulaması.
9. Backend security + quality hardening.
10. V35/V53 legacy cleanup.

## Harness checkpoint
- `scripts/room_live_smoke.py`: iki session + room + gift/balance + iki istemcili room WS chat; auth `/v1/users`; local websocket proxy bypass düzeltmesi commitlendi.
- `scripts/room_ban_live_smoke.py`: ban → liste → 403 rejoin → unban → rejoin.
- `scripts/dm_live_smoke.py`: iki session + conversation create/list + message send/history.
- `scripts/cosmetic_live_smoke.py`: catalog → balance → purchase → ownership → apply.
- Harnessların kod sözleşmesi kontrol edildi; çalıştırılmadan PASS yazılmayacak.

## CI checkpoint
- Quality workflow'a local live E2E jobu daha önce eklendi; gerçek E2E PASS sonucu henüz doğrulanmış değil.
- `runner-recovery.yml` ile gerçek GitHub-hosted runner doğrulaması yapıldı.
- Bazı eski workflow run'larının `jobs: []` ile failure görünmesi ayrı CI registration/job-generation problemi olarak takip ediliyor.
- Railway deploy yok.

## Kural
- Canlı/local E2E yapılmadan özellik tamamlandı sayılmayacak.
- Railway production deploy yalnızca kullanıcı açıkça `deploy` dediğinde yapılacak.
- Pages kök nedeni log/API kanıtı olmadan tahmin edilmeyecek.
- E2E PASS kanıtı olmadan PASS/TAMAM denmeyecek.
