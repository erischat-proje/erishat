# ErisChat Son Durum — 2026-09-17

## Çalışma fazı
- Repo: `semihvarolymk-png/erischat`, branch: `main`.
- Faz: BASELINE / TAM ÇALIŞAN SİSTEME HAZIRLIK.
- Öncelik: mevcut kodu ve canlı temel akışları tamamlamak; yeni ürün özelliklerine baseline çıkışından sonra geçmek.
- Railway production deploy kullanıcı açıkça `deploy` demeden yapılmayacak.

## Son gerçek runner kanıtı
- Runner Recovery #74: run `35256506469`.
- `live-e2e` job `105321368901`: **SUCCESS**.
- Setup, Python, bağımlılık kurulumu, local API ve `/ready`: SUCCESS.
- Anonymous session smoke: SUCCESS.
- Room smoke: SUCCESS.
- Room ban smoke: SUCCESS.
- DM smoke: SUCCESS.
- Cosmetic smoke: SUCCESS.
- İki istemcili room WebSocket history/chat broadcast gerçek runner'da SUCCESS.
- Ban → 403 rejoin → unban → yeniden join gerçek runner'da SUCCESS.
- DM conversation → send → history gerçek runner'da SUCCESS.
- Cosmetic catalog → balance → purchase → ownership → apply gerçek runner'da SUCCESS.
- Runner Recovery `probe` job `105321368404` failure verdi; bu uygulama E2E job'u değildir ve probe adımındaki runner teşhis kontrolüdür.

## CI
- Quality workflow v4'e Python compileall + frontend JavaScript `node --check` + zorunlu frontend kaynak kontrolleri eklendi.
- [ ] Quality v4'ün güncel main commitindeki gerçek runner sonucu ayrıca doğrulanacak.

## Pages kök nedeni
- Son Pages run `35256506397` failure.
- Deploy job `105321366191` runner step'lerine girmeden environment admission aşamasında durdu; job steps yok.
- Daha önce gerçek diagnostics API çıktısıyla `github-pages` environment custom deployment branch policy'sinin yalnızca `gh-pages` olduğu doğrulandı; `main` izinli değil.
- Pages diagnostics guard bu policy engelini görünür hale getiriyor.
- [ ] GitHub Settings → Environments → `github-pages` deployment branch policy içinde `main` izinli hale getirilecek.
- [ ] Sonrasında Pages deploy tekrar gerçek runner'da doğrulanacak.
- [ ] Canlı Pages HTTP 200/index doğrulaması yapılacak.
- [ ] Canlı Pages → Railway API zinciri doğrulanacak.

## Kod / cleanup checkpoint
- `room-live.js` API fallback düzeltmesi mevcut.
- Eski self-modifying/patch ve duplicate Pages workflow'larının gerekli olmayanları temizlendi.
- `yapilanlar.txt` ve `yapilacaklar.txt` yeni runner kanıtıyla güncellendi.
- Yeni özellik geliştirmesi şimdilik durduruldu; kalan baseline maddeleri kapatılacak.

## Sıradaki işlem sırası
1. Quality v4 gerçek runner sonucunu doğrula.
2. Pages environment `main` admission engelini çöz.
3. Pages deploy SUCCESS doğrula.
4. Canlı Pages HTTP + frontend → Railway API doğrula.
5. Kalan frontend/security/cosmetic render kontrollerini kapat.
6. Tüm baseline çıkış kriterleri tamamlanınca ürün geliştirmesine dön.

## Kural
- Gerçek runner/canlı kanıt olmadan PASS/TAMAM veya "tam çalışan" denmeyecek.
- Railway deploy yalnızca kullanıcı açıkça `deploy` dediğinde yapılacak.
